// Start by creating a variable `message`
var name = "habiiba";
var nameLength = name.length;
var message =
  " hello my name is " + name + ". It has " + nameLength + " characters.";
console.log(message.trim());
